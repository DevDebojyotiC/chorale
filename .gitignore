node_modules/
dist/
data/
*.log
.env
.env.*
!.env.example

# Large evaluation clones — keep the report, not the repos
eval/hermes-agent/
eval/openclaw/

# Demo apps built by the coder agent
demo-todo/

# crash dumps
*.stackdump

# tesseract.js OCR language data (downloaded at runtime)
*.traineddata

# Grand-tour runner build output
grand-tour-output/

# Fullstack capability experiments (build output, not source)
experiments/
